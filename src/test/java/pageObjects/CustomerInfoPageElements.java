package pageObjects;

import holders.DriverHolder;
import org.openqa.selenium.WebDriver;

public class CustomerInfoPageElements extends DriverHolder {
    public CustomerInfoPageElements(WebDriver driver) {
        super(driver);
    }
}
